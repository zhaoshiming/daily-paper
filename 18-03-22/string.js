// string  需要理解的是字符串作为原始字符串，是不存在添加到字符串里的，你可以先转数组

charAt() // 完全可以使用数组下标代替

concat()  // 连接字符串，返回新的字符串 

slice(start, end)  == substr    // 使用slice就可以了，不会改变原来的字符串，返回剪切之后的字符串

indexOf()

lastIndexOf()

trim()  返回新的字符串，不改变原来字符串

match()  匹配字符串

split()  转数组







































